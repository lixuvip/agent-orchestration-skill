interface:
  display_name: "Agent Orchestration"
  short_description: "Lightweight native coordination for Codex agents and tasks"
  default_prompt: "Use $agent-orchestration with the least coordination needed: preflight owner capabilities, prefer native subagent/task/worktree status, disclose task visibility, reject stale scope, close every user requirement with current evidence, audit active work before final, and load automation guidance only for recurring work."
